# Mkfd v3 implementation ledger

This is the single execution-status record for `docs/mkfd-v3-implementation-roadmap.md`. Keep evidence compact; do not paste Claude transcripts or duplicate feature-readiness prose here.

## Status meanings

- **Not started:** no implementation slice has begun.
- **In progress:** at least one slice is active or complete, but packet exit criteria are not met.
- **Blocked:** an explicit dependency or product decision prevents progress.
- **Verification:** implementation is complete and packet-wide exit checks are running.
- **Ready:** packet exit criteria have passed with linked evidence.

## Packet status

| Packet | Owner | Status | Active slice | Exit evidence | Outstanding risk |
|---|---|---|---|---|---|
| 0 — Scope/documentation lock | Codex | Ready | — | ADR 0001; 31/31 feature specs and plans; evidence-based `PROGRESS.md`; source/decision consistency check | None; replacement decisions require a new approved ADR |
| 1 — Quality/dependency baseline | Codex | Verification | — | All four slices GREEN; `verify:core` green; Windows `test:e2e` 24 pass / 8 reasoned skips / 0 fail; `bun audit --audit-level=high` clean; dual-OS `quality.yml` and SHA-pinned `supply-chain.yml` added | Dual-OS CI and clean-install reproducibility are structurally asserted but not yet observed on a real GitHub runner; `tdd:claude` launcher has two known defects |
| 2 — Trust boundary/security | Codex | **Ready** | Packet 3 (brief next) | `p2-auth-trust-boundary` and `p2-selector-playground-isolation` both closed: A2 loopback/fail-open auth bypass fixed, timing-safe passkey, login throttling, origin/CSRF guard, request-derived Secure cookie, webhook ingress exception; A1 opaque-origin iframe, sanitized proxy output, self-hosted SelectorGadget with SRI, nonce+source-authenticated postMessage. `verify:core` 1245/1245, `test:e2e` 40 passed / 8 skipped / 0 failed | **Packet 2 complete**: all seven slices closed — auth trust boundary, selector-playground isolation, protected-value AES-GCM, /configs/* removal, CSP and security headers, container hardening, redacting logger |
| 3 — Runtime/config/network platform | Codex | In progress | Packet 3 convergence | Shared outbound/source/settings/browser contracts and all five S6 resource-control slices are GREEN | Retry/fallback/concurrency and route-decomposition parity remain before Packet 3 exit |
| 4 — Frontend platform/My Feeds | Codex | Not started | — | — | Depends on Packets 2–3 contracts |
| 5 — Builder contract/parity | Codex | Not started | — | — | Depends on Packets 2–3 contracts |
| 6 — Source Assistant/web intelligence | Codex | Not started | — | — | Depends on Packets 3–5 |
| 7A — Remote sources | Codex | Not started | — | — | Depends on shared runtime/UI contracts |
| 7B — Ingress/local sources | Codex | Not started | — | — | Depends on shared runtime/UI contracts |
| 8 — Templates/catalog | Codex | Not started | — | — | Depends on protected values and config contract |
| 9 — Service connectors | Codex | Not started | — | — | Depends on source registry and outbound executor |
| 4D/9D — Front-end design assessment/polish | Codex | Not started | — | — | Design tooling installed (impeccable, emilkowalski/skills, motion, morphicons); 4D assessment gates on Packet 4 start, 9D polish must follow Packet 4 route-splitting or it compounds the 944 kB bundle advisory |
| 10 — Release proof | Codex | Not started | — | — | All earlier packets |

## TDD slice evidence

Add one row when a slice begins. Update the same row through RED, scrutiny, GREEN, and broader verification.

| Slice | Packet / findings | Requirements brief | Test-author session | Accepted test files | RED evidence | GREEN evidence | Broader verification | Commit/PR | Migrations / remaining risk |
|---|---|---|---|---|---|---|---|---|---|
| `p0-scope-lock` | Packet 0 | Documentation-only governance bootstrap; no production behavior | Not applicable | Not applicable | Not applicable | Not applicable | Document consistency review | Working tree | Must be Ready before Packet 1 production changes |
| `p1-cross-platform-e2e-launch` | Packet 1 / C12, H3 | `docs/tdd/p1-cross-platform-e2e-launch.md` | `2dde5c4a-ceb9-40cb-88ca-8e9fbd7b77eb` (`claude-sonnet-5`) | `tests/e2e-harness-config.test.ts`; `frontend/e2e/fixtures.ts` | 17 pass / 19 intended fail after fixture handoff | 36 pass / 0 fail; accepted hashes verified | Windows `bun run test:e2e`: 12/12 pass; targeted Biome pass | Working tree | Production auth hardening remains Packet 2 scope |
| `p1-static-quality-contract` | Packet 1 / C3, H2 | `docs/tdd/p1-static-quality-contract.md` | `5841fa29-90ea-4297-9594-1eb398e4d3d8` (`claude-sonnet-5`) | `tests/static-quality-scripts.test.ts`; `tests/static-quality-biome-config.test.ts`; `tests/static-quality-typescript-config.test.ts`; `frontend/e2e/fixtures.ts` | Final accepted RED: 85 pass / 54 intended fail | 139 pass / 0 fail; accepted hashes verified | Targeted Biome on 8 changed test/production files: zero warnings/errors; prior E2E 36/36 | Working tree | Exact Biome 2.5.11 / TypeScript 5.7.3 pins and both lockfiles updated; application diagnostics intentionally move to the next slice |
| `p1-static-diagnostics-cleanup` | Packet 1 / C3, H2 | `docs/tdd/p1-static-diagnostics-cleanup.md` | `801db20e-5c32-44bd-a095-a77a7184ef7f` (`claude-sonnet-5`) | `tests/static-diagnostics-cleanup-architecture.test.ts`; `tests/static-diagnostics-cleanup-anti-bypass.test.ts`; `tests/frontend-accessibility-semantics.test.ts`; `tests/frontend-hook-dependency-safety.test.ts`; `tests/regex-and-callback-correctness-preservation.test.ts`; `tests/protected-value-cast-boundary.test.ts` | Final accepted RED: 354 pass / 15 intended fail / 369 tests | 369 pass / 0 fail / 714 assertions; accepted hashes verified | Aggregate lint/typecheck green (545-warning/13-info cap preserved); 133/133 related runtime tests; production build succeeds | Working tree | Maintained declaration packages added; request-profile/selector/webhook type drift corrected; Selector Playground and FlareSolverr retained; warning-only debt remains owned by later packets |
| `p1-dependency-ci-security-baseline` | Packet 1 / B4, H1-H3, S7, S10 supply-chain | `docs/tdd/p1-dependency-ci-security-baseline.md` | `86ea0a94-339e-4b03-8735-75ab60102d7c` (`claude-sonnet-5`) | `tests/ci-quality-workflow.test.ts`; `tests/ci-supply-chain-workflow.test.ts`; `tests/dependency-audit-exceptions-policy.test.ts`; `tests/dependency-lockfile-resolution.test.ts`; `tests/dependency-manifest-policy.test.ts`; `tests/docker-frozen-install.test.ts`; `tests/existing-feed-parser-security.test.ts`; `tests/playwright-mobile-accessibility-wiring.test.ts`; `frontend/e2e/accessibility.spec.ts` | Round 1: 63 pass / 65 intended fail. Final accepted RED: 113 pass / 75 intended fail | 188 pass / 0 fail on the accepted suite; accepted hashes verified | `verify:core` green (lint 0 errors at the locked 545/13 ceiling; typecheck 0 errors; 995/995 tests; catalog validated; build succeeded); Windows `test:e2e` 24 pass / 8 reasoned skips / 0 fail across both projects; `bun audit --audit-level=high` clean | Working tree | 5 review rounds. Fixed 3 real contrast defects (`--muted-foreground`, `SettingRow` row opacity, new `--wb-warning-ink`). Two product gaps recorded, not patched: no SPA deep-link fallback (Packets 2-3) and Health/Settings unreachable at 390 px (Packet 4). `--wb-warning` still used as text in `SourceAssistantPanel`/`MyFeedsPage` |
| `p1-fallow-static-analysis-gate` | Packet 1 follow-up / B4, S10 static analysis | `docs/tdd/p1-fallow-static-analysis-gate.md` (+ Amendment 1) | `db3144a2-e11c-47c2-b781-40c77e7413f2` (`claude-sonnet-5`) | `tests/fallow-static-analysis-gate.test.ts`; `tests/fallow-analyze-script-and-ci-workflow.test.ts`; `tests/static-analysis-exceptions-policy.test.ts`; `tests/fallow-circular-dependency-behavior.test.ts`; `tests/source-assistant/starter-configs.test.ts` | Round 1: 98/28. Round 3 (post-amendment): 21 RED. Round 4: 11 RED | 192 pass / 0 fail across all five accepted files | `verify:core` green (lint 0 errors at the locked 545/13 ceiling; typecheck 0 errors; 1186/1186 tests; catalog validated; build succeeded); all five slice locks verified | Working tree | 4 review rounds. fallow dead-code 96 to 57 issues, unused files 22 to 5, maintainability 88.8 to 89.9. `fallow --ci` does NOT gate despite its docs; only `--fail-on-issues` does. CI gates changed files via `fallow audit --gate new-only`; `bun run analyze` remains the honest full-pipeline local command and still exits 1 against the residual backlog |
| `p2-auth-trust-boundary` | Packet 2 / A2, audit `:428` loopback-CSRF, S1/S2 session+passkey | `docs/tdd/p2-auth-trust-boundary.md` | `2edd570c-9ea1-4324-9543-e7670b387841` (`claude-sonnet-5`) | `tests/auth-trust-boundary.test.ts`; `tests/auth-connection-info-boundary.test.ts`; `tests/auth-passkey-timing-safety.test.ts` | Round 1: 12 pass / 16 fail. Round 2 (conditional Secure added): 15 pass / 17 fail | 34 pass / 0 fail across the accepted files | `verify:core` green: lint 0 errors at the locked 545/13 ceiling, typecheck 0 errors, `bun test tests/` 1220 pass / 0 fail (pre-slice baseline 1186/0), catalog validated, build succeeded; all six slice locks verify unchanged | `c0eef46`, `25662d1` | 4 review rounds. Lock refreshed twice, both for test defects, both recorded in the decisions section. Known gaps carried: post-lockout login recovery untested (needs a fake clock across a process boundary), and the trusted-proxy Secure path proven only negatively until a trusted-proxy config surface exists |
| `p2-selector-playground-isolation` | Packet 2 / A1 proxy-iframe origin escape, S3 sanitization, S6 self-hosted assets, requirement 5 postMessage auth | `docs/tdd/p2-selector-playground-isolation.md` | `2de2a461-99f5-495f-96c7-3fa824266de1` (`claude-sonnet-5`) | `frontend/e2e/selector-playground-isolation.spec.ts`; `tests/selector-playground-destinations.test.ts`; `tests/selector-playground-proxy-isolation.test.ts` | Round 1: 7 pass / 18 fail, independently reproduced | 25 pass / 0 fail backend, plus 8 pass / 0 fail in the browser | `verify:core` green: 1245 pass / 0 fail, lint at the locked 545/13 ceiling, typecheck clean, catalog validated, build succeeded; `test:e2e` 40 passed / 8 skipped / 0 failed across 48; all seven slice locks verify | `34d4ced`, `c45880a`, `cb2cd31` | 3 review rounds. Lock refreshed once, for a test defect (a positive round-trip that posted into the iframe instead of from it, which no correct implementation could pass). Round 3 also exposed CF-10: the browser suite had been executing a stale prebuilt bundle, so these contracts had never run against their implementation until now |
| `p2-protected-value-aes-gcm` | Packet 2 / S5 protected-value encryption | `docs/tdd/p2-protected-value-aes-gcm.md` | `a1d30f30-3bb8-45fd-a7c4-de1021158781` (`claude-sonnet-5`) | `tests/security-utility-aes-gcm.test.ts`; `tests/protected-value-migration.test.ts`; `tests/protected-value-rotation.test.ts`; `tests/protected-values-key-boundary.test.ts`; `tests/startup-encryption-key-validation.test.ts`; `tests/deployment-encryption-key-default.test.ts`; `tests/helpers/protected-value-fixtures.ts` | Round 1: 1262 pass / 20 fail. Round 2 (after refusal assertions were made identifiable): 1258 pass / 24 fail | 1299 pass / 0 fail | `verify:core` green: 1299 pass / 0 fail, lint at the locked 545/13 ceiling, typecheck clean, catalog validated, build succeeded; `test:e2e` 40 passed / 8 skipped / 0 failed; all eight slice locks verify | (this commit) | 2 review rounds. The helper `tests/helpers/protected-value-fixtures.ts` was added to the lock set by me: it holds the frozen legacy-cipher fixture, so leaving it unlocked would leave a lever that changes what the locked tests mean |
| `p2-configs-static-serving-removal` | Packet 2 / `/configs/*` static serving | `docs/tdd/p2-configs-static-serving-removal.md` | `38b5973d-6ba5-428b-9d4b-9422451ef1df` (`claude-sonnet-5`) | `tests/configs-static-serving-removal.test.ts` | 1314 pass / 2 fail (17 new tests; 15 are deliberate compatibility guards for a removal slice, each checked individually for vacuity) | 1316 pass / 0 fail | `verify:core` green: 1316 pass / 0 fail, lint at the locked 545/13 ceiling, typecheck clean, catalog validated, build succeeded; `test:e2e` 40 passed / 8 skipped / 0 failed; all nine slice locks verify | (this commit) | **1 review round — accepted on the first draft, the only slice so far to need no revision.** Traversal cases are sent over raw `node:http` rather than `fetch`, because the WHATWG URL parser normalizes `..` client-side and would have made them assert the client, not the server |
| `p2-csp-security-headers` | Packet 2 / app-wide CSP and security headers | `docs/tdd/p2-csp-security-headers.md` | `0a50e7ca-a06a-4c08-9d1d-685484d72046` (`claude-sonnet-5`) | `tests/security-headers-baseline.test.ts`; `tests/security-headers-hsts.test.ts`; `tests/security-headers-playground-csp-unchanged.test.ts`; `frontend/e2e/security-headers.spec.ts` | 1330 pass / 14 fail backend, plus 8 browser failures across 2 projects | 1344 pass / 0 fail backend, 56 passed / 8 skipped / 0 failed browser | `verify:core` green: 1344 pass / 0 fail, lint at the locked 545/13 ceiling across 315 files, typecheck clean, catalog validated, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all ten slice locks verify | (this commit) | 2 review rounds; accepted on the first draft. **The author was denied all verification — 19 permission denials covering verify:static, lint, tsc, its own tests and its own Playwright spec — yet produced compiling, deterministic, ceiling-clean tests.** Lock refreshed once for an unsatisfiable assertion (a console-message check no implementation could pass) |
| `p2-container-hardening` | Packet 2 / container hardening; closes CF-11 | `docs/tdd/p2-container-hardening.md` | `fae0bd0f-93b0-42f0-b689-feb8223773ed` (`claude-sonnet-5`) | `tests/docker-container-hardening.test.ts` | 41 pass / 15 fail in the new file; 1381 pass / 19 fail overall, of which 4 were the locked static gate breaking | 1400 pass / 0 fail | `verify:core` green: 1400 pass / 0 fail, lint at the locked 545/13 ceiling with zero errors, typecheck clean, catalog validated, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all eleven slice locks verify | (this commit) | 2 review rounds. Round 1 was substantively right but broke the locked static gate — a NUL byte used as a glob sentinel (error-level lint) plus two dollar-brace test titles. The author had been denied `verify:static`, `lint` AND `bunx biome check`, the third slice running with that denial pattern, so it could not see the breach |
| `p2-redacting-logger` | Packet 2 / redacting logger (final Packet 2 slice) | `docs/tdd/p2-redacting-logger.md` | `63e4f85d-bb8d-4b43-96c1-bd237db010c6` (`claude-sonnet-5`) | `tests/log-redaction.utility.test.ts`; `tests/log-redaction-call-sites.test.ts`; `tests/log-redaction-regression-guard.test.ts` | 1406 pass / 8 fail (14 new tests; the utility file failed to import, so its 491 lines had never executed and were read line by line rather than trusted) | 1469 pass / 0 fail | `verify:core` green: 1469 pass / 0 fail, lint at the locked 545/13 ceiling with zero errors, typecheck clean, catalog validated, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all twelve slice locks verify | (this commit) | 1 review round, accepted on the first draft. **The first attempt at this slice hit a nested session limit** (`is_error: true`, 40 turns, 9k output tokens, zero files written) — the sixth such failure |
| `p3-shared-outbound-executor` | Packet 3 / shared outbound HTTP(S) executor; S3/S4/S6 | `docs/tdd/p3-shared-outbound-executor.md` | `a87bcf8e-5bf1-4e7b-a52a-32d88ca257fd` (`claude-sonnet-5`, 152k output tokens over 186 turns — the largest run on the project) | `tests/outbound-executor.utility.test.ts`; `tests/outbound-network-primitives-architecture.test.ts`; plus one migration suite per target (preview-generator, data-handler, sitemap, calendar-feed, graphql-feed, selector-suggestion, feed-updater-worker) | 1489 pass / 17 fail | 1506 pass / 0 fail | `verify:core` green: 1506 pass / 0 fail, lint **below** the locked ceiling at 542 warnings / 10 infos with zero errors (removing three duplicate redirect loops took real debt with it), typecheck clean, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all thirteen slice locks verify | (this commit) | 1 review round, accepted on the first draft — the third slice running to do so |
| `p3-source-definition-registry` | Packet 3 / one authoritative source-definition registry | `docs/tdd/p3-source-definition-registry.md` | `6adee542-b6fc-45e4-9886-ddf9bef4ee41` (`claude-sonnet-5`, 114k output over 144 turns) | `tests/feed-source-registry.utility.test.ts`; `tests/feed-source-registry-dispatch.test.ts`; `tests/feed-source-registry-preview-support.test.ts`; `tests/feed-source-registry-worker-and-routes.test.ts`; `tests/feed-config-type-compatibility.test.ts` | 1528 pass / 7 fail | 1600 pass / 0 fail | `verify:core` green: 1600 pass / 0 fail, lint at 542 warnings / 10 infos with zero errors (unchanged, below the locked 545/13 ceiling), typecheck clean, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all fourteen slice locks verify | (this commit) | 1 review round, accepted on the first draft — the fourth slice running to do so. Fixed a real pre-existing data-loss bug the suite exposed: the normalizer dropped the entire `webhookFeed` block on every save/load round trip |
| `p3-v2-golden-round-trips` | Packet 3 / V2-01/02/04/06/10/13/14/16 golden round trips + strict per-type validation | `docs/tdd/p3-v2-golden-round-trips.md` | `bb437bae-0346-4e28-bbbb-171758ece79f` (`claude-sonnet-5`, 73k output over 100 turns) | `tests/p3-v2-golden-round-trips.test.ts` (55 tests) | 10 pass / 45 fail | 1655 pass / 0 fail | `verify:core` green: 1655 pass / 0 fail, lint at 542 warnings / 10 infos with zero errors (unchanged, below the locked 545/13 ceiling), typecheck clean, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all fifteen slice locks verify | (this commit) | 2 review rounds. Seven findings fixed, one retired with evidence. Every one was a silent data-loss bug on the edit/save path |
| `p3-settings-substrate-reconciliation` | Packet 3 / settings vs app_settings, managed migrations, readiness, backup/restore | `docs/tdd/p3-settings-substrate-reconciliation.md` | `4b499f81-55a5-42d4-82cb-d4b5bc1fa52b` (`claude-sonnet-5`, 73k output over 77 turns) | `tests/p3-settings-substrate-reconciliation.test.ts` (18 tests) | 6 pass / 12 fail | 1673 pass / 0 fail | `verify:core` green: 1673 pass / 0 fail, lint at 542 warnings / 10 infos with zero errors (unchanged, below the locked 545/13 ceiling), typecheck clean, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all sixteen slice locks verify | (this commit) | 2 review rounds. Lock refreshed once for a test defect — an assertion that threw on the absence of a header a correct implementation must not send. The suite's requirement-7 test parses the backup/restore documentation and calls the functions it names, so doc drift fails the test |
| `p3-settings-runtime-consumers` | Packet 3 / settings that actually change enforced runtime behaviour | `docs/tdd/p3-settings-runtime-consumers.md` | `cca36d0c-248b-473e-91f7-b946341ab4c7` (`claude-sonnet-5`, 73k output over 80 turns) | `tests/p3-settings-runtime-consumers.test.ts` (25 tests) | 15 pass / 10 fail | 1698 pass / 0 fail | `verify:core` green: 1698 pass / 0 fail, lint at 542 warnings / 10 infos with zero errors (unchanged, below the locked 545/13 ceiling), typecheck clean, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all seventeen slice locks verify, including the nine frozen executor files this slice reached inside | (this commit) | 2 review rounds. Lock refreshed once for a single `as any` that breached the anti-bypass gate |
| `p3-flaresolverr-adapter` | Packet 3 / one approved way to reach FlareSolverr, with the endpoint validated as its own outbound target | `docs/tdd/p3-flaresolverr-adapter.md` | `2f9c7d61-4a3b-4f8e-9c02-8b5a1e77d3c4` (`claude-sonnet-5`) | `tests/flaresolverr-adapter-shared-behavior.test.ts`, `-static-guard`, `-data-handler`, `-selector-suggestion`, `-preview-generator`, `-worker`, `-feed-config-route-adapter` (30 tests across 7 files) | 1711 pass / 17 fail overall; 13 pass / 17 fail in the new files | 1728 pass / 0 fail | `verify:core` green: 1728 pass / 0 fail, lint at 540 warnings / 10 infos with zero errors (two fewer than the previous slice, from deleting the duplicated FlareSolverr payload code; below the locked 545/13 ceiling), typecheck clean, catalog validated, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all eighteen slice locks verify | (this commit) | 1 review round, accepted. Implementation caught two of my own defects: a settings safe-fallback that ignored the env allowlist when no database exists, and a `catch` in `fetchSampleHtml` that swallowed policy refusals into an empty page |
| `p3-browser-adapter` | Packet 3 / the browser half of the adapter bullet: subresource interception, per-navigation validation, one session budget | `docs/tdd/p3-browser-adapter.md` | `1396d245-026c-4baf-a6e5-1b5a20b07a6a` (`claude-sonnet-5`, 136 turns authoring + 38 revising) | `tests/browser-adapter.test.ts` (29), `-static-guard` (17), `-data-handler` (6), `-preview-generator` (6), `-worker` (2), plus the locked harnesses `tests/helpers/fake-browser.ts` and `tests/helpers/browser-adapter-worker-preload.ts` | 12 pass / 18 fail / 1 error across the five files, reproduced independently by the lead | 1787 pass / 0 fail | `verify:core` green: 1787 pass / 0 fail, lint at 540 warnings / 10 infos with zero errors (unchanged — the seven new files added none), typecheck clean, catalog validated, build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all nineteen slice locks verify | (this commit) | 1 review round. Two defects returned: eight Biome errors the author's sandbox would not let it check (fourth slice running with that denial), and a static guard that passed vacuously when its walk found no files. A first revise attempt was killed by a session limit having landed nothing, and was relaunched in the same session |
| `p3-request-body-limits` | Packet 3 / S6 inbound request-body resource boundary | `docs/tdd/p3-request-body-limits.md` | `/root/p3_request_body_tests` (`gpt-5.6-luna`, maintainer-authorized substitute while Claude weekly usage is exhausted) | `tests/p3-request-body-limits.test.ts` (16 tests) | 6 pass / 10 intended fail / 47 assertions after revision, independently reproduced by the lead | 16 pass / 0 fail / 106 assertions; accepted hash verified after the webhook-state compatibility delta | Lint and typecheck clean; catalog validation and production build succeeded; `verify:core` reached 1802 pass / 1 pre-existing readiness timeout (the isolated readiness file is 18/18 with its restart case taking 6.3–7.1s against the default 5s); `test:e2e` 56 passed / 8 skipped / 0 failed; all twenty-one slice locks verify | (this commit) | 2 original review rounds plus one compatibility delta: after managed webhook SQLite landed, Luna replaced two obsolete JSONL reads with equivalent isolated SQLite state assertions. The boundary runs before session/auth/parser work, counts encoded bytes, stops unknown-length streams on the first excess byte, and replays accepted bodies unchanged |
| `p3-parser-input-bounds` | Packet 3 / S6 parser and document resource boundaries | `docs/tdd/p3-parser-input-bounds.md` | `/root/p3_request_body_tests` (`gpt-5.6-luna`, same separated test-author session) | `tests/p3-parser-input-bounds.test.ts` (23 tests) | 10 pass / 13 intended fail / 71 assertions after revision, independently reproduced by the lead | 23 pass / 0 fail / 117 assertions; accepted hash verified | Compatibility corpus 24/24 and fallow architecture gate 100/100; lint clean at 540 warnings / 10 infos, typecheck clean, catalog and production build succeeded; full core reached 1824 pass / 2 fail: the known 5s readiness timeout plus one new unused export, which was de-exported and the gate rerun green; readiness is 18/18 at 15s with restart taking 7.4s; `test:e2e` 56 passed / 8 skipped / 0 failed; all twenty-two slice locks verify | (this commit) | 2 review rounds. The first draft was returned for empty-input compatibility, invalid JSON-LD candidates consuming the 20-attempt budget, and proof that an oversized block never reaches `JSON.parse`. Exact UTF-8 byte ceilings now apply before RSS/Atom/JSON Feed, sitemap, ICS, HTML and JSON-LD parsing; oversized JSON-LD blocks are skipped best-effort while later valid blocks remain usable |
| `p3-safe-user-patterns` | Packet 3 / S6 safe user-provided regex execution | `docs/tdd/p3-safe-user-patterns.md` | `/root/p3_request_body_tests` (`gpt-5.6-luna`, same separated test-author session; maintainer-authorized substitute while Claude weekly usage is exhausted) | `tests/p3-safe-user-patterns.test.ts` (40 tests); migrated assertion in `tests/feed-item-filter.test.ts` | 8 pass / 32 intended fail / 105 assertions focused; compatibility 76 pass / 1 intended fail, independently reproduced by the lead | 40 pass / 0 fail / 255 assertions focused; compatibility 77 pass / 0 fail / 119 assertions; accepted hashes verified | Typecheck and lint clean; fallow architecture gate 100/100; full core reached 1865 pass / one known readiness 5s timeout, then isolated readiness passed 18/18 at 15s with restart taking 7.7s; catalog validation and production build succeeded; `test:e2e` 56 passed / 8 skipped / 0 failed; all twenty-three slice locks verify | (this commit) | 3 substantive review rounds plus one mechanical formatting return. RE2JS provides the shared linear-time engine; exact pattern, candidate, and rule-count limits fail with sanitized errors, and compiled matchers are reused for the operation |
| `p3-webhook-event-state` | Packet 3 / S6 webhook field, rate, persistence, retention, and JSONL migration limits | `docs/tdd/p3-webhook-event-state.md` | `/root/p3_webhook_event_tests` (`gpt-5.6-luna`, maintainer-authorized substitute while Claude weekly usage is exhausted) | `tests/p3-webhook-event-state.test.ts` (31 tests) | Existing baseline: 25 pass / 0 fail / 46 assertions across `tests/source-types.test.ts` and `tests/auth-trust-boundary.test.ts`; accepted focused RED: 2 pass / 29 intended fail / 113 assertions | 31 pass / 0 fail / 350 assertions in 2.73s under the default timeout; accepted hash verified | Compatibility 96/96; lint/typecheck clean; catalog and production build pass; `test:e2e` 56 passed / 8 skipped / 0 failed; full-core aggregate was contaminated by the already-authored next-slice RED and known readiness timeouts, with the one new slow test then optimized and independently verified | (this commit) | 1 full semantic review, 2 requirement deltas, and 1 performance-only fixture revision. Events now use bounded validation, header-only auth, fixed per-slug rate limiting, transactional SQLite dedupe/retention, idempotent recoverable JSONL copy-forward, restart-safe reads, and SQLite-backed output regeneration |
| `p3-filesystem-runtime-controls` | Packet 3 / S6 filesystem authorization, bounded traversal/extraction, and transactional state | `docs/tdd/p3-filesystem-runtime-controls.md` | `/root/p3_filesystem_tests` (`gpt-5.6-luna`, maintainer-authorized substitute while Claude weekly usage is exhausted) | `tests/p3-filesystem-runtime-controls.test.ts` (16 tests) | 0 pass / 16 intended fail; independently reproduced with no fixture, syntax, timeout, or checkout-state mutation failure | 16 pass / 0 fail / 288 assertions; accepted hash verified | Focused compatibility 70/70 and config/catalog compatibility 43/43; typecheck clean, lint/canonical catalog/build pass; full core reached 1910 pass / 3 known readiness timeouts under the default 5s, while the isolated readiness suite passed 18/18 at 15s; `test:e2e` 56 passed / 8 skipped / 0 failed | (this commit) | 3 review rounds. Canonical approved-root authorization now guards every save path; traversal, sidecars, reads, extraction, time, and item counts are hard bounded; unsafe I/O and cancellations roll back; SQLite replacement is atomic and restart-safe; legacy JSON copy-forward is bounded, automatic, and idempotent |
| `p7b-imap-reconnect-policy` | Packet 7B / IMAP connection lifecycle, pulled forward for reported bug [#77](https://github.com/TBosak/mkfd/issues/77) | `docs/tdd/p7b-imap-reconnect-policy.md` | `f3aea8c3-4c71-492b-b798-0b616a324b44` (`claude-sonnet-5`, 34 turns authoring + 20 revising) | `tests/p7b-imap-reconnect-policy.test.ts` (23 tests) | 0 pass / 1 fail / 1 error — the suite could not resolve `node/imap-reconnect-policy`, reproduced independently by the lead | 23 pass / 0 fail | `verify:core`: 1923 pass / 33 fail, lint 534 warnings / 7 infos with zero errors (exactly the pre-slice baseline), typecheck clean; the delta against the 1902/31 baseline is +23 new passes, and the failure count drifts 31-33 between runs because the pre-existing Windows `EBUSY` teardown in `p3-webhook-event-state` reports 9 to 11 depending on run context. Nothing outside its own test imports the new module, so it has no production coupling yet. All twenty-seven slice locks verify | (this commit) | 1 review round. **Provider pairing: degraded (rung 2)** — Codex was out of usage, so Claude Opus led and Claude Sonnet 5 authored; role separation held but both seats were the same provider, so this slice got a weaker adversarial pass than a cross-provider split. Returned two items: `I1` had no direct test (it was only enforced passively by the fake-timer harness throwing unless exactly one timer was pending), and four `noNonNullAssertion` warnings had taken project lint from 534 to 538 against a 545 ceiling |


## Decisions taken under delegated judgment

Recorded when the maintainer delegated ordinary roadmap judgment (2026-09-04, overnight). Each entry states the call, the alternatives, and the reasoning, so the trail is reviewable without reading chat scrollback.

| Date | Decision | Alternatives considered | Reasoning |
|---|---|---|---|
| 2026-09-10 | Used GPT-5.6 Luna as the separated test author for `p3-request-body-limits`, retaining the test-only write boundary and independent lead review. | Stop all roadmap work until Claude Sonnet 5 weekly usage resets, or let the lead write the tests. | The maintainer explicitly authorized Luna as the temporary test author because Claude usage is exhausted. Keeping authorship separated preserves the protocol's substantive control: Luna wrote and revised only the new test file, while the lead wrote the brief and review, rejected the incomplete first draft, independently reproduced RED, locked the accepted hash, and never edited the tests. |
| 2026-09-10 | Mounted one streaming byte boundary before sessions, authentication, and route parsers, with stricter caps selected from method and exact trusted path classes. | Add limits independently to each JSON/form/webhook parser, or rely only on `Content-Length`. | Per-parser limits would miss unknown and future routes and would still allow session/auth work before rejection. `Content-Length` alone is attacker-controlled and absent for chunked uploads. The shared boundary rejects known oversize immediately, measures actual encoded bytes when reading, buffers no more than the applicable cap, and replays accepted bytes so existing Hono parsers and v2 behavior remain unchanged. |
| 2026-09-10 | Enforced sitemap, calendar, and JSON-LD drill-page response ceilings both in the approved outbound executor request and again at the parser boundary. | Add only parser guards because all calls already use the approved redirect-safe executor; or add only transport caps. | The executor intentionally has no default response-size ceiling when called directly, so parser-only checks would still buffer an unbounded remote response before rejecting it. Transport-only limits would leave direct parser entry points and future callers exposed. The paired checks stop network growth early and retain a local invariant immediately before expensive XML, event, and DOM parsing. |
| 2026-09-04 | The branch is NOT pushed. Packet 1 remains at Verification rather than Ready. | Push overnight so the dual-OS CI finally executes and Packet 1 could reach Ready by morning. | Pushing publishes to a public repository. Delegated judgment over roadmap work is not authorization to publish, and the push had already been flagged twice as the maintainer's call. The cost of waiting is one packet status field; the cost of publishing unasked is not recoverable. |
| 2026-09-04 | Session cookie `Secure` is conditional on TLS, not unconditional. | Always `Secure` (as the authored test demanded); always `Secure` with a documented opt-out flag. | Maintainer decided this one directly. Unconditional `Secure` would prevent plain-HTTP LAN installs from storing a session cookie at all, making login impossible for a deployment model Mkfd explicitly supports. Playwright would still have passed, since browsers treat localhost as a secure context, so the breakage would have shipped invisible to CI. |
| 2026-09-04 | Added an adversarial case the brief did not ask for: a forged `X-Forwarded-Proto: https` from a non-trusted peer must not cause a `Secure` cookie to be issued. | Accept the conditional-`Secure` requirement as written. | Deriving `Secure` from a client-controllable header without a trusted-proxy check lets any client trick the server into issuing a cookie the browser then refuses to return, locking the user out of their own instance. Consistent with the brief's rule that forwarded headers inform transport and logging, never authorization. |
| 2026-09-05 | Parking a production-file edit mid-run does NOT avoid the launcher boundary abort on Windows. The rule is now absolute: make no production edit while any launcher run is in flight. | Write the edit, then `git checkout --` it before the run finishes so the before/after snapshots match. | Tried and it failed. The launcher hashes file bytes, and `git checkout --` restored the ledger under git CRLF normalisation, which did not match the LF bytes originally written. The hash still differed and the run still aborted. Reverting is not a reliable undo for a content hash on this platform. |
| 2026-09-05 | Relaunched the p2-auth-trust-boundary revision rather than treating it as done. | Accept the round-1 tests as they stand and proceed to lock. | The revision never executed: the nested Sonnet 5 session hit its own usage limit at turn 17 and returned "You have hit your session limit, resets 2:40am". Both review gaps remain open. Accepting would have locked a suite that breaks the Packet 1 warning ceiling and that mandates an unconditional Secure cookie, which would prevent plain-HTTP LAN installs from logging in at all. |
| 2026-09-05 | Treated 3 failing accepted tests as a TEST defect and returned them to Claude, rather than changing the implementation to satisfy them. | Adjust index.ts until the three pass; or edit the locked helper directly. | Verified against a live server that the implementation is correct: a login emits two Set-Cookie headers both named "session" (hono-sessions writes a pre-next sid cookie, then the persisted data after next), and the helper reads only the first via headers.get. Using the last cookie gives auth=true and 200; using the first gives 302. Changing production code to satisfy a helper bug would have broken working auth. Editing the locked test myself is forbidden by AGENTS.md, so it went back to the same Sonnet 5 session. |
| 2026-09-05 | Kept X-Forwarded-Proto untrusted for the Secure-cookie decision. | Honour it whenever present, which is what most reverse-proxy setups expect. | There is no trusted-proxy configuration surface yet. Honouring it unconditionally would let any client force a Secure cookie that the browser then refuses to send back over plain HTTP, locking the user out of their own instance. The positive "a configured trusted proxy upgrades to Secure" path is deferred to whichever slice introduces that config surface. |
| 2026-09-05 | Refreshed the p2-auth-trust-boundary test lock after implementation, which is normally forbidden. | Leave the lock as-is and accept 3 permanently failing tests, or change index.ts to satisfy them. | The round-3 change fixed a helper that made three tests incapable of detecting what they asserted: each failed with a 302 indistinguishable from the redirect an unauthenticated request already receives, so they were a false negative then and a latent false positive later. No assertion was relaxed, no expectation adjusted, no requirement changed, and the implementation was written before that round and not modified to suit it. Recorded here because re-locking is exactly the kind of step that should never be silent. |
| 2026-09-05 | Fixed my own `as unknown as` double cast in loginThrottleKey rather than adjusting the locked anti-bypass baseline. | Raise the double-cast baseline in the locked test to accommodate it. | The guard exists precisely to stop new unsafe casts entering production. Raising the ceiling to fit my own shortcut would have defeated the control on its first real test. `getConnInfo(c).remote?.address` is properly typed and needs no cast. |
| 2026-09-05 | Returned the feed-history cross-test pollution to the test author instead of editing tests/feed-history.test.ts. | Adjust feed-history.test.ts to tolerate a DB-backed store, or drop the in-process import of index.ts from the auth test. | feed-history.test.ts is correct as written and is not part of this slice; changing it would hide the leak rather than fix it. Dropping the in-process import would lose the only means of reaching the indeterminate-address case, which is the shipped defect the slice exists to close. The right fix is containment: restore the global feed-history store after the import. |
| 2026-09-05 | Recorded nested session limits as the dominant throughput constraint, and made checking for them the FIRST step of every scheduled wake-up. | Treat each failed run as a one-off and simply relaunch. | Three runs so far returned `is_error: true` with "You have hit your session limit" and did no work at all, each after ~20 minutes of wall clock. The failure is deceptive: the launcher exits non-zero and looks like a boundary abort or crash, so without inspecting `is_error` and the result text it reads as "the run happened and something went wrong" rather than "nothing happened". Ordering the check first is what prevents a limited run being mistaken for a completed one. |
| 2026-09-05 | Confirmed the self-hosted SelectorGadget asset is served from `utilsRouter`, alongside `/proxy`. | Serve it through the generic static mounts in `index.ts`. | `/configs/*` static serving and the app-wide header work are explicitly separate Packet 2 slices. Keeping the asset on the same router as the route that injects it makes this slice self-contained and avoids a cross-slice dependency for a single file. |
| 2026-09-05 | Confirmed that a redirect to a policy-blocked target must return 403, not the current 500, and that this is in scope for the A1 slice. | Treat the 500 as acceptable, since the policy re-check already runs and does block the request. | Requirement 8 asks for the existing 403 refusal behaviour and for the policy to be re-applied on redirect. Blocking with a 500 is technically safe but indistinguishable from a server fault, which hides a security refusal behind an error and makes it untestable as a policy decision. The fix is small and belongs with the requirement it serves. |
| 2026-09-05 | Vendored the entire SelectorGadget dependency chain (6 files), not just the entry script, and patched `eval()` out of its loader. | Vendor only `selectorgadget.js` as the brief literally said, and pin it with SRI. | The upstream entry file is a 1.7 kB loader that fetches five more assets from CloudFront and googleapis at runtime. Pinning only it would satisfy the letter of "self-host and integrity-pin SelectorGadget" while leaving the feature broken offline and still dependent on two third-party hosts — the exact property the requirement exists to remove. Separately, the loader polled for readiness with `eval("typeof " + name)`, which any CSP without `unsafe-eval` blocks; it now reads `window[name]`, which is equivalent for the names it is called with. Both changes are documented in the vendored file itself. |
| 2026-09-05 | Suspended the recurring roadmap ticks and stopped rather than resolving the CF-09 lock conflict autonomously. | Change the auth lock to expect `/public/`; or change the e2e fixture to expect `/`; or leave the browser suite red and continue to the next slice. | Either fix means editing an accepted test lock, which is explicitly reserved for the maintainer, and the two locks belong to different slices. It is also a product question — where a user lands after login — not purely a test-mechanics one. Continuing to the next slice with the entire Playwright suite red would have buried a real regression behind green `verify:core` output, which is exactly how this went unnoticed in the first place. |
| 2026-09-05 | CF-09 resolved by the maintainer: login redirects to `/public/`, and the auth lock is corrected to match. | Change the e2e fixture to expect `/`; or leave the browser suite red. | `/public/` is where the SPA actually lives — Vite `base` and `BrowserRouter basename` both say so — so `"/"` lands outside the router basename and the app never mounts. The fixture was already right; the auth lock encoded the pre-existing broken behaviour because login was unreachable behind the loopback bypass when it was written. Routed through the owning Sonnet 5 session rather than edited directly. |
| 2026-09-05 | Made `test:e2e` run `bun run build` before Playwright. | Leave the build implicit and rely on whoever runs the suite to remember; or switch the SPA to be served by Vite in dev. | The browser suite was testing a stale prebuilt bundle, so A1 iframe isolation and postMessage authentication had never executed against their implementation — the exact risk flagged when this slice was locked. Serving from Vite was tried and rejected: the backend `/public` proxy is what enforces the session gate, so it made the fixture skip login and run the whole suite unauthenticated (observed: title `Feed Builder` instead of `Enter Passkey`, `/api/feeds` redirecting to `/passkey`). Building first keeps the auth gate and tests real code. |
| 2026-09-06 | Legacy AES-CBC values are now decrypted with `node:crypto` rather than `node-forge`, and the plaintext must be valid UTF-8. | Keep forge on the legacy read path, matching the original code exactly. | Forge’s PKCS#7 padding check is lenient enough that a **wrong** key was accepted about one time in five, almost always yielding an empty string, which the pre-v3 code returned as if it were the secret. That is how the rotation test failed intermittently against my first implementation: rotation would decrypt a foreign-key value to "" and re-encrypt that nonsense over a real secret. node’s CBC validates the full padding and throws. Measured after the change: 0 wrong-key accepts in 5000 trials, down from ~18%, with every legacy round-trip still byte-exact. |
| 2026-09-06 | `/configs/*` removal answers 404 rather than falling through to the SPA catch-all. | Let the catch-all serve index.html for `/configs/...`, which the locked suite would also have accepted. | The catch-all would answer 200 with the SPA for any `/configs` path, which tells a prober the prefix is still handled and makes the requirement-1 body assertions depend on index.html never containing a marker string. A plain 404 is the honest answer for a path that no longer exists. Verified after implementing: authenticated `/configs/x.yaml` returns 404, anonymous returns 302 to the auth gate, and `/public/feeds/*` still answers 404 directly rather than redirecting — so the removal did not catch the anonymous feed output. |
| 2026-09-06 | `Referrer-Policy` is `strict-origin-when-cross-origin`, not `no-referrer`. | `no-referrer`, which is in the locked suite’s own list of safe policies and passed all 28 backend tests. | `no-referrer` also suppresses the `Origin` header on form submissions: Chrome sends `Origin: null`, and the CSRF guard from `p2-auth-trust-boundary` rejects it, so POST `/passkey` returned **403 in a browser** while `curl` — which sets `Origin` itself — still got its 302. `verify:core` was green throughout; only Playwright caught it, which is exactly the failure mode CF-09 was filed for. The browser suite went from 6 passed to 54 on this one-line change. |
| 2026-09-06 | Pinned the base image to a digest read from the Docker Hub registry API rather than one I composed. | Invent a plausible 64-hex digest, which the locked test would have accepted. | The locked test only checks the digest’s *shape*, so a fabricated value passes the suite and then breaks every real build. `sha256:93b7f5ea…bd799` is `oven/bun:1.2.2-debian` as published 2025-02-01. Recorded because it is a case where the tests genuinely cannot protect the implementer, and the honest move is to go and get the real value. |
| 2026-09-06 | Shipped a `redact()` helper plus fixes at the two leaking call sites, rather than a logger module with a 175-call-site migration. | Introduce a `logger` module and migrate every `console.*` call. | Migrating 175 call sites is a mechanical change touching nearly every file, carrying real regression risk for almost no security gain — the overwhelming majority log benign progress. The helper is what makes the next risky call site cheap to write safely. A lint rule banning bare `console.*` was considered and deliberately deferred: it would add warnings against a **locked** ceiling, so it cannot be switched on inside this slice. Filed as a follow-up rather than smuggled in. |
| 2026-09-07 | Put the low-level HTTP primitive in `lib/outbound/pinned-request.ts`, outside `routes/`, `utilities/`, `workers/` and `node/`. | Keep it in `utilities/` and add an exception for it to the architecture ledger. | The locked architecture gate scans those four directories and its exception ledger is pinned at 12 entries, none of which covers the executor — so the only way to satisfy it is for the primitive to live outside the scanned tree. That turned out to be the better design anyway: application code now has no direct network access at all, and the one module that does is small enough to read in a sitting. The test forced a structural improvement rather than a carve-out. |
| 2026-09-07 | Registry lives in `utilities/`, holds handler functions directly, and declares `changeDetection` as `implemented: false`. | Put it in `models/` beside the union; hold string identifiers a separate dispatcher maps; omit the stub entirely. | All three follow the precedent already in the repo (`utilities/service-connector-registry.utility.ts` keeps types in `models/` and the runtime registry with real functions in `utilities/`). Identifier indirection would also have made requirement 3 unprovable without editing a dispatcher too, which is the exact coupling this slice removes. Registering the stub honestly beats omitting it: an entry that says `implemented: false` cannot be mistaken for support. |
| 2026-09-07 | Accepted the author's retirement of V2-16 rather than requiring a test for it. | Insist on a fixture for all eight findings as the brief listed them. | The author verified V2-16 does not reproduce in the backend — `cookies` is assigned identically for every feed type in the caster's shared `base` and survives the round trip — and located the real gap in `frontend/src/components/forms/APIForm.tsx`, which has no cookie UI. That is Packet 5's territory. A fixture that passes on day one is not coverage; a retirement backed by evidence plus one regression-lock test is worth more. First time a slice has done this. |
| 2026-09-07 | `app_settings` survives; the dead `settings` table is dropped by migration 0003 after its rows are carried across, live value winning on conflict. | Rename `app_settings` to `settings`; or drop the dead table without carry-over. | `app_settings` is what every accessor already reads, so renaming churns working code and the migration for no behavioural gain. Carry-over runs before the drop because a dead table with rows in it is still someone's data — and the live value wins because `app_settings` is what the application has actually been reading, making the dead table's copy stale at best. |
| 2026-09-07 | Deliberately reached inside the frozen executor contract to change `getGlobalFetchPolicyOptions` and `resolveFetchPolicy`. | Leave the contract untouched and wire settings somewhere else, or defer the bullet. | The code's own migration note said to: `outbound-fetch-policy.utility.ts` carried "When Settings Page is implemented, these reads must be replaced with effective settings lookups". The freeze protects the policy's *decision logic*, not where its inputs come from, so the scope was narrowed to inputs only — `isBlockedAddress`, the metadata-hostname block and redirect revalidation were untouched, and all nine locked executor test files verify unchanged. |
| 2026-09-07 | `getGlobalFetchPolicyOptions()` stays synchronous and reads SQLite through bun's sync query API. | Make it async, which is the tidier shape for a database read. | The test author found this is close to a hard constraint and produced the evidence: an accepted *locked* test calls it synchronously with no DB row and expects env fallback, and production call sites use it as a default parameter value (`policyOptions: OutboundFetchPolicyOptions = getGlobalFetchPolicyOptions()`), which cannot be awaited. Async would have rippled through every caller and broken a frozen contract for no behavioural gain. |
| 2026-09-07 | An unreadable settings store falls back to the SAFE default, deliberately ignoring the environment. | Fall back to `process.env`, which is what the code did before and looks like continuity. | Precedence is stored > env > default when the store is *readable*. When it is not, honouring `ALLOW_PRIVATE_FETCHES=true` from the environment at exactly the moment the app has lost track of its own configuration is the wrong direction for an SSRF control. The distinction is explicit in the code: a database that cannot be read is not the same as one holding no rows. |
| 2026-09-07 | `feed_run_timeout_ms` is enforced live despite being registered class B, and the registry's default (120000) is now the single fallback. | Honour `restartRequired: true` by ignoring the stored value until restart; keep `DEFAULT_POLICY.feedRunTimeoutMs` (60000) as the fallback. | The setting is read per-run, so there is nothing to defer to a restart — the class B flag governs the Settings UI's messaging, not enforcement. The two defaults disagreed (120000 in the registry, 60000 in fetch-policy), which meant "the default timeout" had two answers depending on which path you asked; making the registry authoritative end to end is the whole point of the slice. |
| 2026-09-07 | Reused `parseRawValue` from the registry instead of the local deserializer I first wrote. | Keep the local switch, which looked simpler. | `string[]` is stored as JSON in the database but supplied comma-separated in an environment variable, and only `parseRawValue` knows both encodings. My duplicate split a JSON array on commas and silently dropped every allowlist entry read from the database — the locked allowlist test caught it. A second deserializer for one type is exactly the duplication this packet exists to remove. |
| 2026-09-07 | `initDb` records a degraded reason instead of throwing when migration fails. | Keep throwing, which is what the code did (its comment said "let's throw to see the error in logs"). | Throwing killed startup, so a bad database was observable only as a restart loop and a stack trace in container logs — nothing could be asked what was wrong. The process now stays up, serves the UI, and reports not-ready through `/readyz` with the reason. Feed serving genuinely does not work in that state, which is why it reports 503 rather than pretending to be healthy. |
| 2026-09-07 | `/readyz` is anonymous — added to `ANONYMOUS_ROUTES` alongside `/public/feeds/*` and `/webhook-feeds/*`. | Put readiness on the existing `routes/health.ts` surface, behind the session gate. | A readiness probe behind the session gate is useless to a container orchestrator, which has no cookie to present. This is a deliberate addition to a deliberately short list, so it is recorded rather than made silently: the endpoint exposes only ready/not-ready and a reason with filesystem paths stripped — no counts, no configuration, nothing an unauthenticated caller could mine. |
| 2026-09-07 | Added `redactPaths` in `lib/analytics/db.ts` rather than reusing `redact()` for the readiness reason. | Reuse `redact()` from the logging utility, as the brief suggested. | `redact()` hides values by *field name*; it cannot touch an absolute path embedded in a free-text error message, which is exactly what a failed migration produces. `redactPaths` is the narrow complement — it strips Windows drive paths and POSIX absolute paths and leaves the diagnostic sentence readable. Recorded because "do not write a second redactor" was a real instruction and this is the reason it did not apply. |
| 2026-09-07 | `runtime_migrations` stays as a separate ledger alongside Drizzle's own. | Fold it into Drizzle's migration table. | It records `migrateLegacyFeedHistory`'s lazy *data* migration, which is a different concern from schema DDL and runs against rows rather than tables. Noted in the review so a future reader does not assume it is redundant and delete it. |
| 2026-09-07 | Accepted the author's reframing of V2-14 from a shape reduction to a plaintext-secret leak. | Test the audit's original framing (rich cookie metadata flattened to name/value). | Direct execution showed cookies pass through the caster and normalizer untouched, so the audit's framing was wrong. The actual defect is worse: a new `{type:"protected"}` cookie value was never run through `protectValue`/`encrypt` the way headers and params were, so its plaintext went straight to the on-disk YAML. Fixed at the caster. |
| 2026-09-07 | Replaced the validator's own hardcoded twelve-type list with `listFeedSourceTypes()`. | Leave it and add only the cross-type block check the tests required. | It was a thirteenth copy of the same knowledge the previous slice consolidated, free to drift from the other twelve. Requirement 6's foreign-block check also derives each type's block name from the registry, so the `webhookFeed` exception is handled in one place rather than re-encoded here. |
| 2026-09-07 | Three separate anti-bypass gates caught my implementation and all three were right. | Raise the ceilings, which three earlier slices did. | (a) Four new `as any` casts in the webhook block took warnings 542 to 546 — replaced with a typed local. (b) An `as unknown as` double cast in the apiMapping migration exceeded the count baseline — replaced by typing the intermediate as both shapes from the outset. (c) Then the *comment* explaining that second fix contained the literal string the gate greps for, pushing the count back to 6. Reworded. Recorded because the third one is genuinely funny and also a real property of a regex-based gate: it counts prose. |
| 2026-09-07 | Added a `sourceBlock` field to the registry and fixed a data-loss bug it exposed. | Leave the normalizer's `input[feedType]` lookup alone; it looked correct. | The locked suite proved `webhookFeed.slug` and `tokenHash` were silently dropped on every save/load round trip. Cause: `webhook` is the one type whose config block name differs from its type id (`webhookFeed`), and the normalizer's fallthrough assumed they matched. That is precisely the class of bug a scattered literal lookup produces and a registry prevents, so the block name is now declared once and read from there. |
| 2026-09-07 | Made `registerFeedSourceType` throw on a duplicate or built-in name rather than overwriting. | Last-write-wins, which is what I implemented first. | The locked tests required it and were right: silent overwrite makes the winner depend on import order, and shadowing a built-in would swap out a real handler with no signal at all. |
| 2026-09-07 | Typed the preview handler input as a declared interface instead of `any`. | `any`, which is what the first draft used. | The locked anti-bypass gate counts `noExplicitAny` diagnostics and refuses growth; it caught the single `any` I added (542 to 543 warnings) and was right to. That gate exists because three earlier slices raised the ceiling rather than fixing the cause. |
| 2026-09-07 | Pinned the connection by rewriting the URL authority and restoring the original hostname in the Host header, rather than via a Node agent `lookup` hook. | The agent `lookup` option, which is the idiomatic mechanism. | Bun ignores it. Verified directly rather than inferred: a `node:http.Agent({ lookup })` aimed at an unresolvable hostname still failed with ECONNREFUSED instead of dialling the supplied address, and axios with the http adapter behaved the same. Rewriting the authority is what actually pins the socket on this runtime, and the locked test — which targets an RFC 2606 `.invalid` hostname that can never resolve — proves it works. |
| 2026-09-07 | Split the policy into `assertAndResolveOutboundTarget` (returns the validated address) and `assertOutboundFetchAllowed` (unchanged void wrapper). | Change `assertOutboundFetchAllowed` itself to return the address, which I did first. | That broke 10 tests in the existing, unlocked `tests/outbound-fetch-policy.test.ts`, which assert `.resolves.toBeUndefined()`. Those tests are not locked so I could have edited them, but they encode an established public contract and nothing about this slice requires changing it. Adding the resolving form alongside left every existing caller and assertion untouched. |
| 2026-09-07 | Deleted three separate local copies of the redirect-following loop (in `selector-suggestion`, `feed-updater.worker`, and inline in `preview-generator`) rather than routing each through the executor in place. | Leave them in place and add policy calls around them. | Two of the three never validated the *initial* URL at all — only redirects — which is precisely the URL an attacker controls. `preview-generator`'s own comment said its duplicate existed only because the shared helper was GET-only, so I added the method-aware form it asked for. Removing them dropped lint warnings from 545 to 542 and infos from 13 to 10. |
| 2026-09-06 | Dropped `auth` from the sensitive-key list, keeping `authorization`. | Redact any field named `auth`, which looks stricter. | An `auth` field is normally a *container* — `proxy.auth = { username, password }` — so redacting it whole hides the username that makes the log useful, while the password inside is already covered by its own name. The locked suite asserts exactly this: `proxy.auth` must survive as an object with its password redacted. Over-redaction destroys the diagnostic value the utility exists to preserve, and the tests were right to pin it. |
| 2026-09-06 | Made the redaction marker constant module-private after the fallow gate rejected it. | Export it for callers, or cite it as an allowed unused export. | `verify:core` went red on the locked `p1-fallow-static-analysis-gate` amendment because nothing imports the constant — the tests assert on a `/redact/i` predicate instead. It was genuinely unused outside the module, so the gate was right and the export was simply wrong. Recorded because it is the fallow gate catching a real surface-area mistake in new code, which is what it was added for. |
| 2026-09-06 | Kept the healthcheck on `/` and made it follow the auth redirect, rather than adding a dedicated health endpoint. | Add a `/healthz` route that answers 200 without a session. | `curl -f` without `-L` treats the 302 to `/passkey` as success, so the check passed against an app that could be entirely broken behind the redirect. Following the redirect fixes it inside this slice’s ownership; a new endpoint would have been an application change this slice does not own, and it is not needed. The same contract is applied to both the Dockerfile HEALTHCHECK and the Compose healthcheck so they cannot drift. |
| 2026-09-06 | Selected the `bun` user the base image already ships instead of creating one. | `useradd` a dedicated uid. | Avoids inventing a uid that then has to be kept consistent with host volume ownership for the `./configs` and `./data` bind mounts, and keeps the Dockerfile smaller. |
| 2026-09-06 | `style-src self unsafe-inline` alongside a strict `script-src self`. | A uniformly strict policy, or nonces/hashes for styles. | 33 files under `frontend/src` use React `style={{ }}`, which emits inline `style` attributes. Worse, adding a nonce or hash to `style-src` makes CSP2+ **ignore** `unsafe-inline` for that directive, breaking every one of them — so the stricter-looking option is actively wrong here. A strict `script-src` with a pragmatic `style-src`, stated plainly, beats a “strict” policy the app cannot run under. |
| 2026-09-06 | Replaced the login page’s Pico CDN stylesheet with inline CSS rather than vendoring it. | Vendor and integrity-pin a local copy, matching the SelectorGadget precedent set in `p2-selector-playground-isolation`. | A deliberate divergence from that precedent: SelectorGadget was vendored because the feature could not work without it, whereas Pico is purely cosmetic for one small form. ~80KB plus an integrity pin to style a single page is disproportionate; inline CSS adds no asset to keep pinned and works air-gapped. The unpinned `@2` CDN link with no SRI sat on the one page every operator sees before authenticating. |
| 2026-09-06 | `frame-ancestors none` + `X-Frame-Options: DENY` everywhere except `GET /proxy`, which is exempt entirely. | Give `/proxy` its own `frame-ancestors self`, which would be stricter. | The locked `p2-selector-playground-isolation` suite asserts `/proxy`’s policy exactly — nothing appended, nothing dropped — so adding any directive there would break an accepted lock. Full exemption is the only choice compatible with it, and `/proxy` is a sandboxed opaque-origin document behind the session gate, which is what actually contains it. The test author worked this constraint out independently; I had not when writing the brief. |
| 2026-09-06 | Accepted the `p2-configs-static-serving-removal` suite on the first round despite 15 of its 17 tests passing before implementation. | Return it for looking vacuous, as an 88%-green suite normally is. | Checked each passing test individually rather than trusting the ratio. This is a removal slice: the passing tests are compatibility guards asserting what must keep being true (traversal stays blocked, the API still does CRUD, protected values stay masked, `/public/feeds/*` stays anonymous), and the two failing tests are exactly the two things that do not exist yet. For a slice that deletes a redundant path, that is the correct shape of RED. |
| 2026-09-06 | Removed the publicly-known Compose defaults for `PASSKEY` and `COOKIE_SECRET` as well as `ENCRYPTION_KEY`, though only the latter is in this slice’s scope. | Fix only `ENCRYPTION_KEY` and file the other two for the container-hardening slice. | A guessable `COOKIE_SECRET` permits session forgery and a guessable `PASSKEY` permits login, so both are at least as severe as the defect I was fixing, and leaving them one line away would have been indefensible. The change is a one-line `${VAR:?message}` per secret. Recorded as CF-11 because only the `ENCRYPTION_KEY` half is covered by a locked test; the container-hardening slice still owes the other two coverage. |
| 2026-09-06 | Added `tests/helpers/protected-value-fixtures.ts` to the slice lock set, which the author had not listed. | Lock only the six test files the launcher reported. | The helper holds `legacyForgeEncrypt`, the frozen copy of the pre-slice cipher that every read-old compatibility test depends on. Left unlocked it would be an editable lever that silently changes what the locked tests actually prove. |
| 2026-09-06 | Derived the AES key as SHA-256 of the whole configured secret. | Keep the pre-v3 derivation of the first 32 raw UTF-8 bytes. | The old derivation made two keys sharing a 32-byte prefix interchangeable and turned a short key into a short AES key; the locked suite requires that two keys sharing a 40-byte prefix not be interchangeable. Hashing uses every byte and always yields exactly 256 bits. The legacy derivation is preserved verbatim on the read-old path, so existing values stay readable. |
| 2026-09-05 | Did not fix the `reuseExistingServer` secret-mismatch defect (CF-10b), despite it corrupting three measurements tonight. | Set `reuseExistingServer: false` for port 5000, which is the correct fix. | `tests/e2e-harness-config.test.ts:480` is an accepted lock asserting exactly `true` for that port. Changing the config to contradict a locked assertion is weakening a lock by another route, and it is reserved for a brief and an authoring round. Filed as CF-10 with a workaround rather than taken unilaterally. |
| 2026-09-05 | Reversed my own round-3 diagnosis rather than defending it. | Report the slice as closed on the round-3 reasoning, which reached a green-looking result. | Round 3 told the author "the implementation is correct, the tests are wrong." The implementation was correct, but the browser was running a different build entirely, so the premise was false and one of the two corrections was justified by luck rather than by evidence. The source/browser disagreement on the `sandbox` attribute is what exposed it. Recorded because a review that reaches the right outcome from a wrong premise is a process failure worth seeing. |
| 2026-09-05 | Included a second, non-obvious correction in the same round: the throttle test's `expect(location).not.toBe("/")` becomes vacuous once success redirects to `/public/`. | Change only the redirect assertion the maintainer asked about. | With the new target, `location` is never `"/"`, so that assertion would pass whether or not lockout works — including against an implementation with no throttling at all, which is exactly what the test exists to prove. Correcting the redirect without this would have left a test that reads as a guard but guards nothing, and the weakening would have been invisible because the suite would go green. |
| 2026-09-05 | Enabled `xfwd: true` on every Vite dev proxy entry, and treat "the proxy must preserve Host or send X-Forwarded-Host" as a documented deployment requirement. | Loosen the CSRF check to accept any Origin when no forwarded host is present; or compare only the path. | Vite rewrites Host to the backend and sends no forwarding headers, so the app could not tell which public origin the browser addressed and refused every state-changing request — the browser suite was 48 failed, all timing out in the login fixture. Loosening the check would have re-opened the CSRF hole that requirement 6 exists to close. Real reverse proxies do send these headers, so enabling them in dev makes development mirror production instead of diverging from it. Browser suite went 48 failed to 4 failed on this change alone. |
| 2026-09-05 | Returned both remaining browser failures to the test author as defects rather than adjusting the implementation. | Make `apiBodyLooksLikeFeedJson` return `false` instead of throwing; and relax the parent listener so a message posted into the iframe is accepted. | Both would have made the implementation worse to satisfy a test. The first failure is `undefined` where `false` was expected, but `undefined` means the hostile page could not issue the request at all — strictly stronger than issuing it and getting a non-feed body. The second posts the message TO the iframe rather than from it, so `event.source` can never be the iframe; accepting it would mean dropping the source check that makes the nonce meaningful. The isolation assertions either side of the first failure both pass, which is what confirms the implementation is correct. |
| 2026-09-05 | Widened the CSRF origin comparison to accept the Host and X-Forwarded-Host the client actually addressed, not just the URL this process sees. | Keep comparing only against `new URL(c.req.url).host`. | The original comparison broke every reverse-proxy deployment: behind nginx the browser sends the public origin while the app is reached on an internal host, so every state-changing request would have returned 403 and the app would have been unusable. Verified directly — `origin: http://localhost:5173` against a server on `:5000` returned 403 before the fix. X-Forwarded-Host is safe to consult here specifically: a cross-site form post cannot set it, and a fetch that tried would need a CORS preflight this app never approves. The locked evil-origin and substring-containment cases still return 403. |
| 2026-09-08 | The adapter returns a normalised `{ html, status }` rather than the raw FlareSolverr `solution` object. | Return `solution` unchanged so call sites keep their existing field access. | Six call sites each reached into `solution.response` themselves, which is exactly the duplication this slice exists to remove; passing the raw object through would have kept the vendor's response shape spread across the codebase and left every future field rename a six-file edit. The narrow return type also stops the callers that only want HTML from accidentally depending on anything else. |
| 2026-09-08 | Distinguished an *uninitialised* settings database from a *degraded* one in `effective-settings.utility.ts`, adding a third state mid-slice. | Leave the two-state read, which failed closed whenever `getDb()` threw. | The safe-fallback was written for a corrupt database and correctly ignores the environment there — a security control whose storage has failed must not honour `ALLOW_PRIVATE_FETCHES=true`. But it also fired for every context that simply never called `initDb`: CLI scripts, the worker before startup, and unit tests, silently discarding the env allowlist that had configured them until now. I checked the locked `p3-settings-runtime-consumers` suite first and confirmed it pins only the *corrupt* case, so splitting the states weakens nothing it asserts; that suite re-verifies at 25/25. |
| 2026-09-08 | A policy refusal now propagates out of `fetchSampleHtml`; only transport failures are swallowed. | Keep the single `catch` returning an empty string for everything, which is the shape my first migration had. | Two accepted tests require a refusal, and they are right: the pre-migration code deliberately kept its policy check *outside* the try for this reason. Turning "this endpoint is not allowed" into an empty sample page hides an SSRF refusal behind what looks like a site that returned nothing, which is the failure mode most likely to get the guard quietly worked around. |
| 2026-09-08 | `feed-config-route-adapter.utility.ts`'s FlareSolverr call was treated as in-scope for this slice. | Defer it to the route-decomposition slice, where that file is otherwise owned. | The slice's premise is that exactly one approved path to FlareSolverr exists; leaving a seventh `axios.post` behind would have made the static guard's "no direct FlareSolverr calls" assertion false the moment it was written, and that guard is the part of this slice that keeps the invariant true later. |

| 2026-09-08 | Accepted the test-only `_launchBrowser` injection hook the author added to `BrowserFetchRequest`. | Require module mocking (`mock.module("patchright")`) everywhere, as the three call-site suites already do, and keep the production type free of test affordances. | The project already carries exactly this idiom — `_dnsLookupFn` and `_skipDns` on `OutboundFetchPolicyOptions` — so this is an established convention rather than a new one, and the adapter's own suite needs per-test control over launch behaviour that whole-file module mocking cannot give cleanly. The author raised it as an open question rather than sliding it in, which is the behaviour I want to reinforce. |
| 2026-09-08 | Locked `tests/helpers/fake-browser.ts` and `tests/helpers/browser-adapter-worker-preload.ts` alongside the five test files, rather than only the files named `*.test.ts`. | Lock the test files alone, as every prior slice has done. | The lock hashes exactly the `testFiles` list, and the fake-browser harness carries real test logic — what a route verdict is, when a navigation resolves, what the page reports. An unlocked helper would let a later implementation pass by weakening the harness instead of the assertions, without breaking a single lock. Every prior slice's helpers were fixtures; this one's is a behavioural stand-in, so the same reasoning that locks a test locks it. |
| 2026-09-08 | Kept the drill-chain cookie gap out of `p3-browser-adapter` and filed it as CF-14. | Fix it inside this slice, since the migration is already touching all three advanced branches and the inconsistency is one line to close. | `resolveDrillChain`'s advanced branch ignores its `cookies` parameter while the preview and worker branches apply theirs. Closing that during a migration slice would change which hosts receive a user's session cookie — a live behaviour change dressed as a refactor, and precisely the kind that is hard to notice in review. The author flagged it rather than fixing it, which was right. |
| 2026-09-08 | Wrapped Patchright structurally in `browser-adapter.ts` rather than casting it to the adapter's narrow interface. | Take TypeScript's own suggested remedy for the conversion error — a double assertion through `unknown`. | The compiler proposed exactly the escape the project's anti-bypass gate forbids, and the slice's own locked guard fails on that literal string appearing anywhere in the adapter file, comments included. Writing the delegation out by hand is a dozen lines and makes the compiler check the compatibility claim instead of being told to trust it; it immediately caught two real mismatches (`addInitScript` and `route` resolve to a `Disposable`, not `void`). |

## Baseline

- Branch: `major-revision-0526`
- Reviewed baseline: `c8a54df`
- 2026-09-02 `bun run verify:static`: failed during Biome lint with 71 errors and 387 warnings; typecheck did not run because lint failed.
- TDD launcher evidence: Claude Code 2.1.258 is callable; argument, Sonnet 5 identity, path-boundary, same-session revision, and slice-test-lock enforcement have all run against live slices.

## Carried findings (raised by Packet 1, owned elsewhere)

| ID | Finding | Evidence | Owner packet |
|---|---|---|---|
| CF-01 | No SPA deep-link fallback. `index.ts` serves `/public/*` through `serveStatic({ root: "./" })` and maps only `GET /` to `index.html`; `frontend/vite.config.ts` proxies `/public` to that backend. Refreshing or bookmarking any client route returns a bare `404 Not Found`. | `page.goto('feeds'\|'health'\|'settings')` renders `404 Not Found` in the browser suite; forced `accessibility.spec.ts` to navigate by in-app clicks only. | 2, 3 |
| CF-02 | Health and Settings have no navigation entry point at 390 px. `Sidebar.tsx` is the only place linking `/health` and `/settings` and is `lg:`-gated; `BottomNav.tsx` exposes only My Feeds, Build Feed, and Catalog. | Eight explicitly-reasoned mobile skips in `accessibility.spec.ts`, `health.spec.ts`, `settings.spec.ts`. | 4 |
| CF-03 | `--wb-warning` (`#8c7f50`) is a fill tone still used as text in `SourceAssistantPanel.tsx` and `MyFeedsPage.tsx`. As small text it measures 3.37:1 on its own 15 % tint and 3.99:1 on white. `--wb-warning-ink` (`#695f3c`) now exists for this; those call sites were left to their owning packets. | Same defect class as the fixed `SettingRow` badge. Those routes pass axe today. | 4, 6 |
| CF-04 | `tdd:claude` launcher, two defects hit during this slice. (1) When it throws after Claude responds, the session-state file is never written and the session ID is orphaned — recovered here from `-last-response.json`. (2) It passes the whole prompt as a single process argument, so a long feedback file fails on Windows with `ENAMETOOLONG` — worked around by splitting per-round delta feedback files. | Both reproduced in this slice. | 1 follow-up slice |
| CF-05 | The test suite rewrites tracked runtime state. `bun run test` mutates `feed-state/filesystem/filesystem-test.json` (`lastSeenAt`/`lastModifiedAt`), so every run dirties the working tree and invites committing machine-specific timestamps. Tests should write feed state to a temporary directory, or the file should be untracked. | Reproduced on every run; restored rather than committed in `35301bb`. | 1 follow-up slice |
| CF-06 | **RESOLVED 2026-09-09** in `1d09052` (`feat(ui): lock in the type system and actually load it`), which locked the maintainer's font decision and closed the defect. The three faces are now self-hosted through OFL variable packages — `@fontsource-variable/geist`, `-geist-mono`, `-figtree` — and reach the build: 13 `@font-face` rules and 13 woff2 files (173 KB) ship from `public/assets/`, with zero CDN requests, which is what the LAN and air-gapped case required. Tokens are `--font-display` (Geist, headings, `letter-spacing: -0.02em`), `--font-sans` (Figtree, body and UI controls) and `--font-mono` (Geist Mono, code and markup previews), mapped into Tailwind's `fontFamily` so `font-sans`/`font-display`/`font-mono` resolve to the tokens rather than Tailwind's default stacks. `Courier New` is gone from `FeedPreview.tsx`. Note for a future slice: the `geist` npm package was tried first and dropped because it ships Next.js font loaders that do not work under Vite. **One sub-item remains open and is deliberately deferred:** the feed preview renders into a `blob:` URL document, which cannot see the parent's `@font-face` rules, so it names Geist Mono first and otherwise falls through to a system mono. Serving the face into that document needs an absolute font URL and interacts with the iframe sandboxing Packet 2 owns, so it was left for that work rather than forced here. | Verified 2026-09-13: 13 `@font-face` in `public/assets/index.css`, 13 woff2 in `public/assets/`, three `@fontsource-variable` packages in `frontend/package.json`, no font link in `frontend/index.html`, `Courier New` present only in an explanatory comment. | 4D (validation only); preview-document font belongs to the Packet 2 iframe sandboxing work |
| CF-07 | Font asset filenames are not deterministic across builds. `bun run build` reassigns the 13 `@fontsource` woff2 subsets to different `indexN.woff2` names on each run with no source change, so every build produces a spurious 13-file binary diff plus a changed `index.css`. Measured: `index2.woff2` was 5812 bytes at HEAD and 7420 after an unrelated rebuild. | Reproduced by running `bun run build` twice with a clean tree in between; the committed set and the rebuilt set are both internally consistent, only the numbering differs. | 1 follow-up, and a Packet 10 release-proof prerequisite |
| CF-08 | Vendoring SelectorGadget brings jQuery 1.3.1 (2009) into the tree. The upstream loader is only 1.7 kB and pulls five further assets at runtime — `selectorgadget.css`, `dom.js`, `diff_match_patch.js` and `interface.js` from CloudFront, plus jQuery 1.3.1 from `ajax.googleapis.com` — so self-hosting the entry file alone would have left five unpinned third-party fetches behind it. All six are now vendored under `public/vendor/selectorgadget/`. jQuery 1.3.1 has known XSS advisories and is not covered by `bun audit`, which only sees declared dependencies. Exposure is bounded by the opaque-origin sandbox this slice introduces: the script runs in a document that cannot reach the app origin, its storage, or its APIs. | Loader inspected directly; all five upstream URLs confirmed and fetched. | 2 (revisit if the playground is ever un-sandboxed), 10 (release notes) |
| CF-09 | **RESOLVED 2026-09-05** (login now redirects to /public/; auth lock corrected via round 5). **Two accepted test locks contradict each other, and the whole Playwright suite is red because of it.** `tests/auth-trust-boundary.test.ts:197` asserts a successful login redirects to exactly `"/"`. `frontend/e2e/fixtures.ts:35` waits for `**/public/` after submitting the passkey. Both are locked, from different slices. The contradiction was latent because the loopback bypass meant login never actually ran: the fixture's title check never saw "Enter Passkey", so it skipped the login branch entirely. Closing A2 made login real and the conflict surfaced — `bun run test:e2e` is 48 failed, every test timing out in the `authenticatedPage` fixture. The SPA is served under `/public/` (Vite `base`, `BrowserRouter basename`), so `/public/` is the behaviourally correct destination, but changing it fails the auth lock and leaving it fails every browser test. Resolving requires editing a lock belonging to a different slice plus a product call on where login lands. | Reproduced directly: `origin`-varied POST to `/passkey` returns 302 with `location: /`; fixture waits for `**/public/` and times out at 30s. `verify:core` stays green throughout because it does not run Playwright. | 2, and a Packet 1 re-verification |
| CF-10 | **The Playwright suite could silently test a stale build, and could silently authenticate against the wrong server.** Two independent defects found while closing A1. (a) `index.ts:343` serves `./public/*` statically and `frontend/vite.config.ts` proxies `/public` to it, but nothing ran `bun run build` before `test:e2e` — so the browser exercised whatever bundle happened to be on disk. The A1 origin-isolation and postMessage contracts had never once run against their implementation. Fixed here by making `test:e2e` build first. (b) `frontend/playwright.config.ts` mints fresh random `PASSKEY`/`COOKIE_SECRET`/`ENCRYPTION_KEY` per run, but sets `reuseExistingServer: true` for port 5000, so a leftover backend from an earlier run is reused holding the *old* passkey: every login fails, five failures trip the new lockout, and the suite dies with 429s that look like an auth regression. **Not fixed** — `tests/e2e-harness-config.test.ts:480` is an accepted lock asserting `reuseExistingServer === true` for 5000, so changing it needs a brief and an authoring round. Workaround until then: kill listeners on 5000/5173 before a run, or export `MKFD_E2E_PASSKEY`. | (a) Received `sandbox` was `["allow-same-origin", "allow-scripts", ...]` while `SelectorPlayground.tsx` declares only `allow-scripts`; `grep -c allow-same-origin public/assets/index.js` = 1, `frontend/src` = 0. (b) Observed 429 on a first login against a leftover backend; three separate measurements tonight were corrupted by it before it was identified. | 1 re-verification, 2 |
| CF-11 | **RESOLVED 2026-09-06** (all three secrets now have locked coverage in `tests/docker-container-hardening.test.ts`, which asserts the `:?` requirement symmetrically via `test.each`). `docker-compose.yml` shipped publicly-known defaults for all three secrets, not just `ENCRYPTION_KEY`: `PASSKEY:-your_passkey_here` and `COOKIE_SECRET:-your_cookie_secret_here`. A known cookie secret permits session forgery and a known passkey permits login, so both are at least as severe as the encryption-key default this slice was scoped to. **Fixed here** — all three now use `${VAR:?message}` so Compose refuses to start rather than substituting a public value — but only the `ENCRYPTION_KEY` change is covered by a locked test. The container-hardening slice should add equivalent coverage for the other two. | `tests/deployment-encryption-key-default.test.ts` asserts only that no `ENCRYPTION_KEY:-` fallback and no `your_encryption_key_here` literal remain. | 2 container hardening |
| CF-12 | **HTTPS connections are validated but not address-pinned.** The DNS time-of-check/time-of-use gap is closed for HTTP: the executor connects to the address validation approved. For HTTPS it is not, because pinning by rewriting the URL authority would present the IP as the TLS SNI and break certificate validation; doing it properly requires driving the connection target and the TLS servername apart, which is its own piece of work with its own failure modes. HTTPS URLs are still fully validated — scheme, metadata block, private ranges, redirect revalidation, credential refusal — so this narrows the gap rather than leaving it open, but a DNS rebind between validation and connection is still possible over TLS. Recorded explicitly because an unstated exemption here would read as done and is not. | `lib/outbound/pinned-request.ts` returns the URL unchanged for any non-http protocol. The locked suite exercises pinning over HTTP only. | 3 (a later executor slice) |
| CF-13 | **The locked architecture exception ledger now carries six stale FlareSolverr entries.** `tests/architecture-boundaries.test.ts` pins the exception list at exactly twelve entries, six of which named the direct `axios.post` FlareSolverr calls this slice removed. Those six now match nothing: the code they excepted is gone, replaced by `lib/outbound/flaresolverr-adapter.ts`, which sits outside the scanned directories by design. The remaining six still describe live code, so the locked non-vacuity check — which requires every scan to find at least one real exception — continues to hold and the suite is green. Recorded rather than edited: the count sits inside an accepted lock, and a stale-but-harmless entry is not grounds to touch one. A future lock refresh should drop the six and reduce the pinned count. | `tests/architecture-boundaries.test.ts` asserts twelve exceptions and a non-vacuous scan; neither assertion notices that six entries are unreachable. | 3 (whichever slice next refreshes the architecture lock) |
| CF-14 | **`resolveDrillChain`'s advanced (browser) branch silently ignores cookies.** `utilities/data-handler.utility.ts` accepts a `cookies` parameter and applies it on the FlareSolverr and plain-HTTP paths, but its Playwright branch never calls `addCookies`, unlike the equivalent branches in `preview-generator.utility.ts` and `feed-updater.worker.ts`, which both do. A drill chain over an authenticated site therefore silently returns logged-out pages in advanced mode while working elsewhere — a correctness gap that reads as a scraping failure. Deliberately not fixed in `p3-browser-adapter`: making it apply cookies changes which hosts receive a user's session cookie, which is a behaviour change needing its own slice and its own tests, not a line added during a migration. | `tests/browser-adapter-data-handler.test.ts` asserts validation and interception for that branch; no test asserts anything about cookies on it, in either direction. | 3 or later (needs its own slice) |
